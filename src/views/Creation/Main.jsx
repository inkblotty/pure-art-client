import React from 'react';

class Creation extends React.Component {

    render() {
        return (
            <div>
                Creation: all three modes with a big canvas
                - In progress (editable)
                - Pending (can review which transactions are done, which ones are not)
                - Complete (can look at the art and transactions that went into it)
            </div>
        );
    }
}

export default Creation;
